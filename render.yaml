# Render.com - Configuration du déploiement
# https://docs.render.com/blueprint-spec
services:
  - type: web
    name: penalty-predictor
    runtime: node
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22
